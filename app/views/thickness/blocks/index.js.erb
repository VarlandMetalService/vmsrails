<% js = escape_javascript(
  render(
    :partial => '/thickness/blocks/list',
    :locals => { :notes => @blocks,
                 :term => params[:with_search_term] }
  )
) %>
$('#blocks').html("<%= js %>");
<% js2 = escape_javascript(
  render(
    :partial => '/thickness/blocks/filter_form',
    :locals => { :notes => @blocks }
  )
) %>

