<p id="notice"><%= notice %></p>
<% provide(:title, 'Thickness Readings') %>
<div class="row mt-3">
  <div class="col">
    <h3 class="mb-0">Thickness Readings</h3>
  </div>
</div>
<div class="row mt-3">
  <div class="col-12" id="filter_form">
    <%= render partial: 'filter_form', cached: :true, locals: { notes: @blocks} %>
  </div>
</div>

<div class="row">
  <div class="col-12" id="list">
    <%= render partial: 'list', locals: { notes: @bbl,
                                        term: params[:with_search_term] } %>
  </div>
</div>
