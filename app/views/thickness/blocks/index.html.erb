<p id="notice"><%= notice %></p>

<h1>Thickness Readings</h1>

<div class="row">
  <div class="col" id="filter_form">
    <%= render partial: 'filter_form', locals: { notes: @blocks} %>
  </div>
</div>

<div class="row">
  <div class="col" id="list">
    <%= render partial: 'list', locals: { notes: @blocks,
                                        term: params[:with_search_term] } %>
  </div>
</div>
