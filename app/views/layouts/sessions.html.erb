<!DOCTYPE html>
<html>

  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">


    <title><%= [content_for(:title), 'VMS'].compact.join(' – ') %></title>

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag     'application',
                                media: 'all',
                                'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag  'application',
                                'data-turbolinks-track': 'reload' %>

  </head>

  <body class="<%= params[:controller] %>-controller">

    <div class="container mt-3 mb-3">
      <div class="row justify-content-center">
        <div class="col-12 col-sm-6 col-md-4 mb-3">
          <%= image_tag 'logo.png', class: 'mw-100' %>
        </div>
        <div class="w-100"><!----></div>
        <div class="col-12 col-sm-6 bg-white rounded yield-content">
          <% flash.each do |message_type, message| %>
            <div class="alert alert-<%= message_type %> mt-3"
                 role="alert">
              <strong><%= message %></strong>
            </div>
          <% end %>
          <%= yield %>
        </div>
      </div>
    </div>

  </body>

</html>