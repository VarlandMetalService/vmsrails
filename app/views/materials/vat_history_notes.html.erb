<% provide(:title, 'Vat History Notes – Material Management System') %>
<div class="row mt-3">
  <div class="col">
    <h3 class="mb-0">Vat History Notes</h3>
  </div>
</div>
<div class="row mt-3">
  <div class="col" id="filter_form">
    <%= render partial: 'filter_form', locals: { notes: @vat_history_notes } %>
  </div>
</div>
<div class="row mt-3" id="vat_history_notes">
  <%= render partial: 'list', locals: { notes: @vat_history_notes,
                                        term: params[:with_search_term] } %>
</div>